name: Deploy to WordPress.org
on:
  push:
    push:
      branches: [ master ]
    pull_request:
      branches: [ master ]
jobs:
  tag:
    name: Deploy in SVN
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name:  Deploy
      run: |
        bash deploy.sh
    env:
      SVN_USERNAME: 'imbasynergy'
      SVN_PASSWORD: 'aUqy*P3sn)6Etju'
      SVN_VERSION: '2.8.5'
      SVN_TEXT_COMMIT: 'Updating self-diagnosis'
      SEC_TEST: ${{ secrets.MY_SECRET }}
