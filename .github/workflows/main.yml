name: Deploy to WordPress.org
on:
  push:
    push:
      branches: [ master ]
    pull_request:
      branches: [ master ]
jobs:
  tag:
    name: New tag
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: My action on commit
      run: |
        echo ${{ github.action_path }}
